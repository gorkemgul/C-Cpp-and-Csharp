#include <stdio.h>
#include <stdlib.h>

int main()
{
    // We're going to cover how to use while loop in c.
    // let's create an integer.
    int a;
    a = 0;
    while (a < 5)
    {
        printf("%d\n", a);
        a++;
    }
}